---
phase: 12-infrastructure-cleanup-hosting
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - website/style.css
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "Dead CSS from the loader, old cyberpunk desktop, old paper/pres layouts, HUD/minimap, and unused keyframe is removed from style.css"
    - "Duplicate panel-header declarations are collapsed into a single authoritative block"
    - "style.css is ~160 lines shorter with zero visual regressions"
  artifacts:
    - path: "website/style.css"
      provides: "Clean stylesheet with dead selectors removed"
  key_links:
    - from: "website/style.css"
      to: "website/index.html"
      via: "CSS selectors match HTML elements still present after Plan 01 cleanup"
      pattern: "\\.win95-desktop|\\.panel-header|\\.paper-section"
---

<objective>
Remove approximately 160 lines of dead CSS from style.css — styles for elements that no longer exist in index.html or are permanently hidden.

Purpose: Eliminate maintenance debt before the module split. Dead CSS is the largest cleanup item (per CODE-AUDIT.md summary table). All dead blocks were identified in the audit.

Output: website/style.css with loader styles, old cyberpunk desktop theme, old paper/pres layout styles, HUD/minimap styles, unused keyframe, and duplicate panel-header declarations removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/CODE-AUDIT.md

<interfaces>
<!-- Dead CSS blocks identified by CODE-AUDIT.md — exact selectors to remove -->

Dead CSS §3 from CODE-AUDIT.md:

Block 1 — Loader subsystem (style.css lines ~63–102, ~40 lines):
Selectors: .loader-inner, .loader-logo, .loader-bar, .loader-fill, .loader-text, .loader-pct
Reason: loader HTML was gutted; only empty div with display:none remains (removed in Plan 01)

Block 2 — Old cyberpunk desktop theme (lines ~506–588, ~80 lines):
Selectors: .desktop, .desktop.visible, .desktop-wallpaper, .desktop-icons, .desktop-taskbar,
           .taskbar-title, .taskbar-user, .taskbar-time, .icon-img
Reason: Pre-Win95 iteration; superseded by .win95-desktop, .win95-taskbar, .icon-emoji

Block 3 — Old paper/pres layouts (lines ~341–412 and ~415–484, ~70 lines):
Selectors: .paper-cards, .paper-card, .paper-card-num, .paper-table, .panel-placeholder,
           .pres-outline, .pres-num, .slide-arch-compare, .arch-compare-item, .arch-label,
           .arch-detail, .slide-timeline-mini, .tl-mini-item, .tl-mini-highlight, .tl-mini-year,
           .slide-video, .slide-chip-compare, .chip-item, .chip-name, .chip-desc
Reason: Old layout variants; current paper uses .paper-section, current pres uses .pres-slide-track

Block 4 — Unused keyframe (line ~1392, ~5 lines):
Selector: @keyframes glitchShift
Reason: Defined but never referenced in any animation property

Block 5 — HUD/minimap system (~90 lines):
Selectors: .hud, .hud-top, .hud-title, .hud-author, .hud-controls, .hud-minimap,
           .minimap-dot, .minimap-portal, .portal-label, .portal-label--paper, etc.
Reason: HUD HTML removed in Plan 01; these selectors now have no targets

Block 6 — Duplicate .panel-header declarations (~15 lines):
Lines ~264–278 define .panel-header + ::before
Lines ~754–761 redefine THE SAME selectors with slightly different values (later block wins)
Action: Remove the earlier duplicate block (lines ~264–278) since the later block is authoritative
Also remove duplicate .panel-eyebrow color rules in lines ~287–289 that are overridden by line ~758
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead CSS — loader, old desktop, keyframe</name>
  <files>website/style.css</files>
  <read_first>
    - website/style.css (read the full file to locate exact line numbers for each dead block before editing)
  </read_first>
  <action>
    Read style.css in full to locate exact current line numbers (the CODE-AUDIT.md numbers are approximate).

    Remove the following blocks by finding them via their selector names:

    **Block 1 — Loader subsystem (~40 lines):**
    Find the comment `/* Loader */` or selector `.loader-inner` and remove all loader-specific rules:
    `.loader-inner`, `.loader-logo`, `.loader-bar`, `.loader-fill`, `.loader-text`, `.loader-pct`
    Do NOT remove `.loader` or `.loader.done` if they still exist — only remove the inner element styles.

    **Block 2 — Old cyberpunk desktop theme (~80 lines):**
    Find the block containing `.desktop` (NOT `.win95-desktop`) and remove these exact selectors and their rule bodies:
    `.desktop`, `.desktop.visible`, `.desktop-wallpaper`, `.desktop-icons`, `.desktop-taskbar`,
    `.taskbar-title`, `.taskbar-user`, `.taskbar-time`, `.icon-img`
    Important: Do NOT remove `.win95-desktop`, `.win95-wallpaper`, `.win95-taskbar`, `.icon-emoji` — those are live.

    **Block 3 — Unused keyframe (~5 lines):**
    Find and remove: `@keyframes glitchShift { ... }`
    Verify it's not referenced by searching for `glitchShift` in style.css and main.js before removing.

    After each removal, confirm the file is still valid CSS (no unclosed braces). Count the opening `{` and closing `}` in the deleted block to confirm they balance before deleting.
  </action>
  <verify>
    ```
    grep -c "\.loader-inner\|\.loader-logo\|\.loader-bar\|\.loader-fill" website/style.css
    ```
    Must return 0.

    ```
    grep -c "\.desktop-wallpaper\|\.desktop-icons\|\.desktop-taskbar\|\.icon-img" website/style.css
    ```
    Must return 0.

    ```
    grep -c "glitchShift" website/style.css
    ```
    Must return 0.
  </verify>
  <acceptance_criteria>
    - `grep -c "\.loader-inner" website/style.css` returns 0
    - `grep -c "\.desktop-wallpaper" website/style.css` returns 0
    - `grep -c "glitchShift" website/style.css` returns 0
    - `grep -c "\.win95-desktop" website/style.css` returns 1 or more (live selector not removed)
    - Opening page in agent-browser shows Win95 desktop styled correctly — no broken layout
  </acceptance_criteria>
  <done>Loader styles (~40 lines), old cyberpunk desktop theme (~80 lines), and unused glitchShift keyframe (~5 lines) removed from style.css.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead paper/pres layouts, HUD/minimap CSS, duplicate panel-header</name>
  <files>website/style.css</files>
  <read_first>
    - website/style.css (re-read after Task 1 changes to get updated line numbers)
    - website/index.html (verify .paper-section, .pres-slide-track, .panel-header are still live)
  </read_first>
  <action>
    Remove the following remaining dead blocks:

    **Block 3 — Old paper/pres layout styles (~70 lines):**
    Find and remove rules for these selectors:
    `.paper-cards`, `.paper-card`, `.paper-card-num`, `.paper-card h3`, `.paper-card p`,
    `.paper-table`, `.paper-table th`, `.paper-table td`, `.paper-table .highlight td`,
    `.panel-placeholder`, `.pres-outline`, `.pres-num`,
    `.slide-arch-compare`, `.arch-compare-item`, `.arch-compare-item--ai`, `.arch-label`, `.arch-detail`,
    `.slide-timeline-mini`, `.tl-mini-item`, `.tl-mini-highlight`, `.tl-mini-year`,
    `.slide-video`, `.slide-video iframe`, `.video-caption`,
    `.slide-chip-compare`, `.chip-item`, `.chip-name`, `.chip-desc`

    Before removing, grep each selector against index.html to confirm zero matches. Do NOT remove `.paper-section`, `.pres-slide`, `.pres-slide-track` — those are live.

    **Block 4 — HUD/minimap CSS (~90 lines):**
    Find and remove all rules for:
    `.hud`, `.hud-top`, `.hud-title`, `.hud-author`, `.hud-controls`,
    `.hud-minimap`, `.minimap-dot`, `.minimap-portal`,
    `.portal-label`, `.portal-label--paper`, `.portal-label--pres`, `.portal-label--exp`
    The `.portal-icon` selector may also be dead — grep index.html to confirm before removing.

    **Block 5 — Duplicate panel-header declarations:**
    There are TWO blocks defining `.panel-header` and `.panel-header::before`. The FIRST occurrence (around line 264–278) is the stale one; the SECOND occurrence (around line 754–761) is the authoritative version (later in CSS cascade wins).
    Remove the FIRST occurrence. Also remove the `.panel-eyebrow` color rules from `.panel-header--paper .panel-eyebrow`, `.panel-header--pres .panel-eyebrow`, `.panel-header--exp .panel-eyebrow` that appear near the first occurrence (lines ~287–289) — they are overridden by the global `.panel-eyebrow` rule near line 758.

    After all removals, verify brace balance and open page in agent-browser.
  </action>
  <verify>
    ```
    grep -c "\.paper-cards\|\.pres-outline\|\.slide-arch-compare\|\.slide-chip-compare" website/style.css
    ```
    Must return 0.

    ```
    grep -c "\.hud-minimap\|\.minimap-dot\|\.minimap-portal\|\.portal-label" website/style.css
    ```
    Must return 0.

    ```
    wc -l website/style.css
    ```
    Must be at least 120 lines fewer than the original 1405 lines (i.e., 1285 or fewer).

    Open page in agent-browser — Win95 desktop, research paper panel, presentation panel all render correctly.
  </verify>
  <acceptance_criteria>
    - `grep -c "\.paper-cards" website/style.css` returns 0
    - `grep -c "\.hud-minimap" website/style.css` returns 0
    - `grep -c "\.portal-label" website/style.css` returns 0
    - `wc -l website/style.css` returns 1285 or fewer (was 1405)
    - `grep -c "\.paper-section" website/style.css` returns 1+ (live selector preserved)
    - `grep -c "\.panel-header" website/style.css` returns 1+ (one canonical block kept, duplicates removed)
    - Opening paper panel in Win95 shows correctly formatted research paper sections
  </acceptance_criteria>
  <done>Dead paper/pres layout styles (~70 lines), HUD/minimap CSS (~90 lines), and duplicate panel-header declarations (~15 lines) removed. style.css is ~160 lines shorter total across Plans 01 and 02.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -c "\.loader-inner\|\.desktop-wallpaper\|glitchShift\|\.hud-minimap\|\.paper-cards\|\.portal-label" website/style.css` → 0
2. `wc -l website/style.css` → 1285 or fewer (≥120 lines removed)
3. Agent-browser: Win95 desktop loads correctly, open paper/pres/exp panels — all styled correctly, no broken layouts, zero console errors
</verification>

<success_criteria>
- All 6 dead CSS blocks removed (loader, old desktop, old paper/pres, keyframe, HUD/minimap, duplicate panel-header)
- style.css is at least 120 lines shorter than the original 1405 lines
- Zero visual regressions — Win95 desktop, paper panel, presentation panel, experience panel all render correctly
- Zero console errors confirmed in agent-browser
</success_criteria>

<output>
After completion, create `.planning/phases/12-infrastructure-cleanup-hosting/12-02-SUMMARY.md`
</output>
