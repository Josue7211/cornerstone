---
phase: 01-library-assignments-2-3-interest-web-submit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assignment-1-interest-web.excalidraw
  - assignment-2-interest-web.png
autonomous: false
requirements: []

must_haves:
  truths:
    - "Interest web has exactly 5 subtopic nodes matching the 5 locked subtopics"
    - "Old subtopics (Specialized AI Chips, Parallel Processing & CUDA, CPU vs GPU vs TPU) are gone"
    - "A PNG export exists and is readable — nodes, arrows, and labels are all visible"
    - "Research focus highlights are on Local/On-Device AI and AI Affordability (the two focus topics)"
  artifacts:
    - path: "assignment-1-interest-web.excalidraw"
      provides: "Updated interest web with 5 correct subtopics"
      contains: "Local"
    - path: "assignment-2-interest-web.png"
      provides: "PNG export for Canvas submission"
  key_links:
    - from: "assignment-1-interest-web.excalidraw"
      to: "assignment-2-interest-web.png"
      via: "PNG export process (browser or script)"
      pattern: "excalidraw.*export|exportToSvg|exportToCanvas"
---

<objective>
Update the interest web Excalidraw file to reflect the 5 locked subtopics, then export it as a PNG for Canvas submission.

Purpose: The current Excalidraw file has the right structure but wrong subtopics — "Specialized AI Chips (TPUs, NPUs)", "Parallel Processing & CUDA", and "CPU vs GPU vs TPU Architectures" need to be replaced with "Local/On-Device AI", "Token Optimization", and "AI Affordability". The file already has "GPU Evolution (Graphics → AI)" and "Future of AI Computing" which are correct and must stay.

Output:
- assignment-1-interest-web.excalidraw — updated with 5 correct subtopics
- assignment-2-interest-web.png — PNG export for submission
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-library-assignments-2-3-interest-web-submit/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Excalidraw JSON with 5 correct subtopics</name>
  <files>assignment-1-interest-web.excalidraw</files>

  <read_first>
    - /home/josue/Documents/homework/cornerstone/assignment-1-interest-web.excalidraw (full file — needed to understand current structure before editing)
  </read_first>

  <action>
Edit assignment-1-interest-web.excalidraw by updating the 3 subtopic nodes that contain old/wrong text. The file is JSON — make surgical text replacements only. Do NOT change positions, colors, IDs, arrow connections, or any element except the text fields listed below.

CURRENT TEXT → NEW TEXT (exact replacements):

1. Element with id="sub2-text":
   - Current "text": "Specialized AI Chips\n(TPUs, NPUs)"
   - Current "originalText": "Specialized AI Chips\n(TPUs, NPUs)"
   - New "text": "Local/On-Device AI\n(AI at home)"
   - New "originalText": "Local/On-Device AI\n(AI at home)"

2. Element with id="sub3-text":
   - Current "text": "Parallel Processing\n& CUDA"
   - Current "originalText": "Parallel Processing\n& CUDA"
   - New "text": "Token Optimization\n(Efficiency)"
   - New "originalText": "Token Optimization\n(Efficiency)"

3. Element with id="sub5-text":
   - Current "text": "CPU vs GPU vs TPU\nArchitectures"
   - Current "originalText": "CPU vs GPU vs TPU\nArchitectures"
   - New "text": "AI Affordability\n(Accessible Hardware)"
   - New "originalText": "AI Affordability\n(Accessible Hardware)"

Also update the "star" highlight labels — the research focus highlights (star1 and star2) currently point to sub1 (GPU Evolution) and sub2 (currently Specialized AI Chips). After the update, sub2 becomes Local/On-Device AI and sub4 is Future of AI Computing. The research focus per CONTEXT.md is LOCAL AI + AFFORDABILITY:

Update element with id="star2":
   - Current position: x=770, y=38 (near sub2 which is top-right)
   - The dashed highlight box "highlight2" is already positioned at x=718, y=35 covering sub2
   - After update, sub2 (top-right) becomes "Local/On-Device AI" — this is correct, keep star2 there

Check: sub5 (bottom-left, x=30, y=530) becomes "AI Affordability". Add a third highlight for it:
   - Add a new element "highlight3" with type="rectangle", x=18, y=505, width=264, height=118, strokeColor="#862e9c", strokeStyle="dashed", strokeWidth=1, opacity=60, fillStyle="solid", backgroundColor="transparent", roughness=1, roundness={"type": 3}
   - Add a new element "star3" with type="text", x=65, y=508, width=160, height=18, text="★ Research Focus", originalText="★ Research Focus", strokeColor="#862e9c", fontSize=13, fontFamily=1, textAlign="center", verticalAlign="top"

These additions mark AI Affordability as a second research focus (alongside Local/On-Device AI), matching the CONTEXT.md locked decisions.

Use the Write tool to save the complete updated JSON to assignment-1-interest-web.excalidraw. Read the file first, make the substitutions, then write the full updated content back.
  </action>

  <verify>
    <automated>grep -c "Local/On-Device AI\|Token Optimization\|AI Affordability" /home/josue/Documents/homework/cornerstone/assignment-1-interest-web.excalidraw</automated>
  </verify>

  <acceptance_criteria>
    - grep "Local/On-Device AI" returns a match
    - grep "Token Optimization" returns a match
    - grep "AI Affordability" returns a match
    - grep "Specialized AI Chips" returns NO match (old subtopic gone)
    - grep "Parallel Processing" returns NO match (old subtopic gone)
    - grep "CPU vs GPU vs TPU" returns NO match (old subtopic gone)
    - JSON is valid (python3 -c "import json; json.load(open('assignment-1-interest-web.excalidraw'))" returns no error)
    - File still contains "GPU Evolution" and "Future of" (correct subtopics unchanged)
  </acceptance_criteria>

  <done>Excalidraw file updated with 5 correct locked subtopics. JSON valid. Old subtopics completely removed. Research focus highlights on Local/On-Device AI and AI Affordability.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Export interest web as PNG</name>

  <what-built>
The Excalidraw file at /home/josue/Documents/homework/cornerstone/assignment-1-interest-web.excalidraw has been updated with the 5 correct subtopics. It needs to be exported as a PNG for Canvas submission.

Automated export attempt: Try using agent-browser to load the file in excalidraw.com and export PNG.

Steps for automated export (executor should try this first):
1. Use agent-browser to navigate to https://excalidraw.com
2. Click the menu (hamburger icon top-left)
3. Click "Open" → upload the .excalidraw file from /home/josue/Documents/homework/cornerstone/assignment-1-interest-web.excalidraw
4. Once loaded, click the menu again → "Export image"
5. Ensure "PNG" is selected, click "Download"
6. The file will download to ~/Downloads/ — move it to /home/josue/Documents/homework/cornerstone/assignment-2-interest-web.png

If agent-browser cannot complete the export (file upload/download limitations in headless browser):
- Note the failure in the log
- The human verification checkpoint below handles this
  </what-built>

  <how-to-verify>
    1. Open /home/josue/Documents/homework/cornerstone/assignment-2-interest-web.png in an image viewer (eog, feh, or Nautilus)
    2. Confirm you can read all 5 subtopic labels: GPU Evolution, Local/On-Device AI, Token Optimization, AI Affordability, Future of AI Computing
    3. Confirm the center ellipse reads "AI Hardware Architecture"
    4. Confirm arrows connect the center to each subtopic
    5. Confirm at least 2 nodes have the "★ Research Focus" dashed purple border

    If the PNG does not exist yet (automated export failed):
    1. Open https://excalidraw.com in your browser
    2. Click the hamburger menu (top-left) → Open → load the file at: /home/josue/Documents/homework/cornerstone/assignment-1-interest-web.excalidraw
    3. Click hamburger menu → Export image → PNG → Download
    4. Save/move the downloaded file to: /home/josue/Documents/homework/cornerstone/assignment-2-interest-web.png
    5. Type "done" when the PNG file exists at that path
  </how-to-verify>

  <resume-signal>Type "done" once assignment-2-interest-web.png exists and all 5 subtopic labels are readable in the exported image. Type "issues: [describe]" if something looks wrong.</resume-signal>
</task>

</tasks>

<verification>
1. grep "Local/On-Device AI" assignment-1-interest-web.excalidraw — must match
2. grep "Specialized AI Chips\|Parallel Processing\|CPU vs GPU" assignment-1-interest-web.excalidraw — must return nothing
3. ls -la assignment-2-interest-web.png — must exist
4. python3 -c "import json; json.load(open('assignment-1-interest-web.excalidraw')); print('JSON valid')" — must print "JSON valid"
</verification>

<success_criteria>
- Excalidraw file has 5 correct subtopics matching the locked decisions in CONTEXT.md
- PNG exported and saved as assignment-2-interest-web.png
- PNG is visually readable — all labels visible, arrows present, center topic clear
- Ready for Canvas upload as Assignment #2
</success_criteria>

<output>
After completion, create .planning/phases/01-library-assignments-2-3-interest-web-submit/01-B-SUMMARY.md documenting:
- Subtopics updated (old → new mapping)
- Whether automated PNG export succeeded or required manual steps
- File path of the PNG export
</output>
